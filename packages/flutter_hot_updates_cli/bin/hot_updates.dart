void main(List<String> arguments) {
  const lines = [
    'flutter_hot_updates CLI',
    '',
    'Phase 0 repository foundation is installed.',
    'Product commands will be added in the CLI milestone.',
  ];

  for (final line in lines) {
    // ignore: avoid_print
    print(line);
  }
}
